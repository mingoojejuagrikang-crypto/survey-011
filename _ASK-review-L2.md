# _ASK — 콜드 리뷰 L2 (데이터·시트 정확성)

산출물: `~/workspace_teamops/deliverables/2026-08-06-survey-011-v0460-cold-review-L2-data.md`

**BLOCKING 없음.** 판정은 전부 실측으로 닫혔다. 아래는 **처방 단계에서 민구 결정이 필요한 것**
1건(NON-BLOCKING)이다 — 내 리뷰는 이 답 없이 완료했다.

---

## Q1 (🟡 NON-BLOCKING) — 수동으로 바꾼 순환 컬럼이 「세션 총 행 수」에서도 빠져야 하나?

### 무엇이 모호한가

판정 항목 2에서 실측한 것:

```
조사나무 seq 1~10 / 조사과실 seq 1~5 상태에서 **조사과실만 '수동'으로** 바꾸면
  총 행 수      = 50 (변화 없음)
  조사나무 값   = 1,1,1,1,1,2,...   ← 같은 값이 5행 연속 시트에 기록된다
  조사과실 칸   = 빈칸               ← FB-A 패치가 뺐다(의도대로)
```

FB-A 패치(`isUserInputColumn`)는 **값 출력**만 막았고 **자릿수 계산**(`isCycling`)은 안 고쳤다.
그래서 「구분자 없는 중복 행 5개」가 생긴다.

고치는 방향은 명확한데, **고쳤을 때의 결과가 의도인지 아닌지를 내가 판단할 수 없다.**

### 후보 (고르지 마라 — 근거와 함께)

**A. `isCycling`도 수동 컬럼을 뺀다** (술어 통일)
- 결과: 위 예에서 **총 행 수 50 → 10**. 조사나무 = 1,2,3,...,10 (중복 없음).
- 근거: FB-A의 논리 그대로다 — "사람이 넣는 칸은 앱이 계산하지 않는다".
- 대가: 이 술어를 **세션 행수 · 세션명 판정(`sessionLabel.ts:34`) · 음성확인 자동 전이
  (`settingsStore.ts:397,404`) · 정정 시 행 점프(`computeRowFromAutoChange`)** 가 공유한다.
  네 곳의 동작이 함께 바뀐다.

**B. 자릿수 계산에서만 뺀다** (`computeTotalRows`·`nestedAutoValue`의 필터만 수정)
- 결과: 위와 같지만 파급이 두 함수에 국한된다.
- 대가: 「사람이 넣는 칸」 술어가 둘로 갈린다 — 다음 회차에 한쪽만 고쳐질 위험.

**C. 손대지 않는다** (v0.46.0 상태 유지)
- 근거: 수동 컬럼을 순환에서 빼면 **이미 기록된 세션의 행 수 해석이 달라진다**.
  되돌리기 비싼 쪽이라면 다음 회차로 미루는 판단이 성립한다.
- 대가: 중복 행이 계속 시트에 올라간다.

### 내 권고

**B로 좁게 고치되, 그 전에 민구가 「총 행 수 50 → 10」이 원하는 결과인지 한 번 봐야 한다.**
행 수는 화면에 보이는 값이고, 조사 계획(나무 10 × 과실 5)과 직접 연결된 숫자다.
*"과실을 수동으로 적을 거니까 나무 10그루만 돌면 된다"* 가 맞으면 B, *"과실 칸을 손으로 채울
거라 50행이 맞다"* 면 C다. **코드가 답할 수 있는 질문이 아니다.**

### 막힌 범위

**없다.** 리뷰 판정은 완료했다. 이 답은 처방 확정에만 필요하다.
