# ❓ WP-D 질문 — 칩 왕복 설정 UI를 **어디에** 둘 것인가

> 레인 `wp-d` · 작성 2026-08-05 · 대상: Larry / 민구
> 상태: 🔴 **BLOCKING 1건**(Q1) + 🟡 NON-BLOCKING 2건(Q2·Q3 — 가정 명시하고 진행 중)

---

## Q1 🔴 BLOCKING — 「칩 왕복 속도」 설정 UI의 배치 위치

### 무엇이 모호한가

민구 R3 원문은 **"입력탭의 진행설정에서 칩 왕복 속도 및 활성화 설정 기능 추가"** 다.
브리핑 §2는 그 섹션을 `SessionOptionsSection.tsx`로 지목하면서 *"실제 노출 위치를 코드로
확인하고 어긋나면 보고해라"* 라고 단서를 달았다. **확인했더니 어긋난다.**

**실측 3건 (집계 명령 포함)**

| # | 실측 | 명령 |
|---|---|---|
| 1 | **「진행설정」이라는 이름의 UI는 이 레포에 없다 — 0건** | `grep -rn "진행설정\|진행 설정" src/ tests/ docs/` → 0 |
| 2 | 브리핑이 지목한 `SessionOptionsSection.tsx`는 **설정탭**에서 렌더된다 | `src/screens/SettingsScreen.tsx:211` |
| 3 | **입력탭에는 별도의 설정 서랍이 실재한다** — `ActiveControlSteppers.tsx` (`data-testid="input-control-panel"`) | `src/components/voice/ActiveControlBar.tsx:211`이 렌더 |

3번이 중요한 이유: 민구가 **08-02에 "바지인 기능 토글을 「입력탭의 서랍메뉴」에 포함"** 이라고
지시해서 말끊기 토글이 들어간 곳이 **바로 그 서랍**이다(`ActiveControlSteppers.tsx:13-15` 주석).
R3의 *"입력탭의 …설정"* 이라는 표현은 **문자 그대로는 이쪽을 가리킨다.**

### 후보 두 안 (고르지 않았다)

**안A — 설정탭 › 세션 옵션 카드** (`SessionOptionsSection.tsx`, 브리핑 지목)

```
┌ 설정탭 (세로 스크롤) ───────────────────┐
│  … 항목 목록 · 항목 추가 …              │
│ ┌ 세션 옵션 카드 ─────────────────────┐ │
│ │ 세션명 항목          [(자동 선택) ▾]│ │
│ │ 세션명               [___________]  │ │
│ │ 세션명 미리보기: 2026-08-05 …       │ │
│ │ 빠른 인식 (실험)             [ ◯]  │ │
│ │ 입력화면 자동 캡처            [◯ ]  │ │
│ │ ┌ 칩 왕복 ───────────────────────┐ │ │
│ │ │ [−]      8초      [+]          │ │ │
│ │ │   편도 8초 · 0초 = 끔          │ │ │
│ │ └────────────────────────────────┘ │ │
│ │ 안내 음성                 [고르기▾]│ │
│ └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

- ✅ 브리핑이 배정한 **내 파일**이라 레인 충돌 0
- ✅ 다른 지속 설정(자동 캡처·빠른 인식)과 같은 자리 = 일관
- ❌ 민구 원문의 **"입력탭"** 과 탭이 다르다
- ❌ 조사 **도중에** 속도를 바꾸려면 탭을 나갔다 와야 한다(세션 중 탭 이동)

**안B — 입력탭 › 조절판 서랍** (`ActiveControlSteppers.tsx`)

```
┌ 입력탭 (조사 중) ───────────────────────┐
│ [항목01 12.3][항목02 —][항목03 —] ← 왕복 │
│                                          │
│            (중앙 인식값)                 │
│                                          │
│ ┌ 허용 인식률 60% · 안내속도 1.05x    ⌄ │ │
│ │ ┌ 허용 인식률 ┐ ┌  안내속도  ┐        │ │
│ │ │[−] 60%  [+] │ │[−] 1.05x[+]│        │ │
│ │ └─────────────┘ └────────────┘        │ │
│ │ ┌ 말끊기        켬              [✓] ┐ │ │
│ │ └───────────────────────────────────┘ │ │
│ │ ┌ 칩 왕복      8초        [−]  [+] ┐  │ │
│ │ └───────────────────────────────────┘ │ │
│ └──────────────────────────────────────┘ │
└──────────────────────────────────────────┘
```

- ✅ 민구 원문 **"입력탭"** 에 문자 그대로 부합
- ✅ 칩존을 **보면서** 속도를 조절한다 — 이 기능은 "멀리서 보이는가"가 판정 기준이라
  보면서 맞추는 게 실제로 맞다
- ❌ 🔴 이 파일은 `ActiveControlBar.tsx`(**WP-G 레인 금지파일**)가 렌더하고,
  **WP-G 과제 자체가 "닫힌 조절판 필이 도트를 덮던 것 해소"** — 즉 **이 서랍의 접힌 토글이
  WP-G의 작업 대상**이다. 지금 만지면 커밋이 겹칠 가능성이 높다
- ❌ 서랍 확장 높이가 늘어난다 → `heroLayout.ts:124`가 접힌 토글 49px을 고정 계산에 쓰고 있어
  레이아웃 계약과 물릴 수 있다(미검증)

### 왜 혼자 못 정하나

- **민구가 볼 화면이고 두 안이 팽팽하다** — 원문 충실성(안B) vs 레인 안전성(안A)이 정면 충돌.
- 안B는 §5 표의 **"남의 파일"** 에 해당한다. WP-G와 같은 파일 계열을 동시에 만지면
  오늘 이미 한 번 난 **레인 중복 사고**와 같은 유형의 충돌이 난다.

### 막힌 범위 / 안 막힌 범위

| | 내용 |
|---|---|
| 🔴 **막힘** | 설정 UI의 **최종 배치 위치**(안B로 가려면 남의 파일 승인 필요) |
| 🟢 **진행 중** | ① `ChipZone.tsx` 왕복 엔진 ② `settingsStore` 필드 1개 ③ 신설 오라클 ④ **안A 위치에 UI 구현** — 셋 다 어느 답이든 공통으로 필요하고, 안A는 브리핑이 배정한 파일이다. 답이 안B면 **UI 블록을 통째로 옮기면 된다**(렌더 위치 이동은 싸다) |

👉 브리핑 §9의 *"왕복을 넣었으면 최소한 끄는 수단은 함께 넣어라"* 를 지키기 위해
**안A 위치에 먼저 붙여 둔다.** 답이 오면 옮긴다.

---

## Q2 🟡 NON-BLOCKING — 「8초」의 정의 (가정 명시하고 진행)

브리핑 §2가 *"편도 기준인지 왕복 1주기인지는 네가 정하고 산출물에 명시"* 로 위임한 축.

**채택 가정: 편도 8초 (왕복 1주기 = 16초).**
근거 — 플랜 `R3` 질문 원문이 *"(제안: **편도** 6~8초)"* 였고 민구 답이 *"기본값은 8초"* 였다.
제안 단위를 그대로 받은 것으로 읽는 게 자연스럽다. 왕복 1주기로 읽으면 편도 4초가 되어
민구가 경계한 *"너무 빠르면 못 읽고"* 쪽으로 기운다.

---

## Q3 🟡 NON-BLOCKING — 07-27 「우측 끝 고정」 회귀 테스트 처리 (가정 명시하고 진행)

정당 파손 전수 grep(`grep -rn "chip-zone\|ChipZone\|우측 끝\|scrollLeft" tests/` = **5파일**)에서
`tests/v039-active-zones.spec.ts`가 우측 끝 계약을 **6개 단언**으로 검증하고 있다(:205 :207 :230
:242 :277 :288 · `fixtures/previewVerify.ts:232`는 칩존 `scrollLeft` 보존을 검증).
기본값 8초(=기본 ON)면 이 단언들이 전부 흔들린다.

**채택 가정: 테스트를 지우지 않는다.** 공유 픽스처 `tests/fixtures/activeZones.ts`의
`SETTINGS.state`에 **`chipSweepSeconds: 0` 한 줄**을 넣어 기존 칩 계약 테스트를 **"왕복 OFF일
때의 계약"** 으로 명시한다.
근거 — 브리핑 §2가 *"폐기가 아니라 대체"* · *"되살리려면 무엇을 봐야 하는지 적어라"* 라고 했다.
**왕복 OFF에서는 07-27 계약이 그대로 유효**하므로, 조건을 명시하는 것이 계약을 보존하는 길이고
단언을 지우는 것보다 정보가 많이 남는다. 72조합 스윕도 왕복이 멈춘 상태에서 재야 측정이 흔들리지
않는다.

⚠️ `tests/fixtures/activeZones.ts`는 **여러 레인이 공유**한다. 한 줄 추가지만 동시 수정 시
충돌하므로 `wlog`로 알리고 넣는다. 다른 레인이 같은 파일을 열고 있다면 알려달라.

---

# ✅ A1 — **안B 확정: 입력탭 조절판 서랍** (민구 선택 08-05)

민구가 ASCII 목업 두 안을 보고 **안B**를 골랐다. 근거는 네가 적은 그대로다 —
*"조사 중에 바로 · 탭 이동 불필요"*, 그리고 **칩존을 보면서 맞춰야 하는 기능**이라는 성질.

🔑 **네 실측이 이 결정을 만들었다.** 브리핑이 지목한 `SessionOptionsSection`은 설정탭이었고,
「진행설정」이라는 UI는 존재하지 않았다. **네가 반증하지 않았으면 민구 원문과 다른 탭에
만들어졌을 것이다.** `[TEAMOPS-44]`의 실사례로 회차 산출물에 올린다.

## 🔴 WP-G와의 경계 — 충돌은 없다. 파일이 다르다

네가 우려한 충돌을 실측으로 갈랐다:

| 레인 | 소유 파일 | 무엇을 |
|---|---|---|
| **WP-D (너)** | **`ActiveControlSteppers.tsx`** | **서랍 「안」의 내용** — 칩 왕복 스테퍼 추가 |
| **WP-G** | `ActiveControlBar.tsx` · `StateDots.tsx` | **접힌 필(오버레이)과 도트 격자** — 서랍 「밖」 |

👉 **`ActiveControlSteppers.tsx`는 이제 네 파일이다.** WP-G에게 금지로 통지했다.
🔴 **`ActiveControlBar.tsx`는 계속 금지다** — 렌더 호출부이고 WP-G의 작업 대상이다.
서랍을 그 파일에서 렌더하는 방식을 바꿔야 한다면 **BLOCKING으로 올려라**(직접 고치지 마라).

### ⚠️ 두 가지 주의

1. **`heroLayout.ts:124`의 접힌 토글 49px 고정 계산** — 네가 발견한 이 계약은 **WP-G 소유**다.
   네 변경이 **펼친 높이**만 늘리면 무관하지만, **접힌 상태의 높이**에 영향이 가면 즉시 보고해라.
   (스테퍼 한 줄 추가는 펼친 높이만 늘리는 게 정상이다 — 그렇지 않으면 그 자체가 신호다.)
2. **서랍이 길어진다.** 지금 항목이 허용 인식률 · 안내속도 · 말끊기 3개인데 4개가 된다.
   **375px(iPhone 13 mini)에서 서랍이 화면을 넘지 않는지** 확인하고 수치를 산출물에 적어라.
   넘치면 그 사실을 보고해라 — 내부 스크롤이냐 재배치냐는 민구 판단이다(BLOCKING).

## ✅ A2 (NON-BLOCKING) — **8초 = 편도. 네 판단이 맞다.**

플랜 R3의 질문 원문이 *"한 바퀴에 몇 초쯤이 멀리서 눈으로 확인 가능한가? **제안: 편도 6~8초**"*
였고 민구가 *"기본값은 8초"* 로 답했다. **제안의 단위를 그대로 받은 것**이라는 네 독해가 옳다.
👉 **UI 설명 문구에 「편도」를 명시해라**(네 목업의 *"편도 8초 · 0초 = 끔"* 그대로). 그러면
다음 사람이 왕복 1주기로 오해하지 않는다.

## ✅ A3 (NON-BLOCKING) — **v039 우측끝 6단언 보존 방식, 그대로 진행해라.**

*"지우지 않고 공유 픽스처에 `chipSweepSeconds: 0` 한 줄로 「왕복 OFF 조건 계약」으로 보존"* —
🟢 **좋은 판단이다.** 계약을 죽이지 않고 **조건을 명시적으로 만든** 것이라 07-27 계약이
「폐기」가 아니라 「OFF일 때의 동작」으로 살아남는다. 되살릴 때도 이 픽스처가 출발점이 된다.
👉 브리핑 §2가 요구한 **주석**(무엇을 대체했는지·왜·되살리려면)을 그 픽스처 옆에도 한 줄 남겨라.

---

# ✅ Q4 — **닫혔다 (2026-08-06).** 아래 질문은 범위가 틀렸다

> 🔴 **이 질문은 「375×667 전용 23px 초과」로 잡혔지만 실제 범위는 전 뷰포트였다.**
> 08-06 민구 제보(*"서랍에서 칩 자동 스크롤 속도 조절할 수 있는 메뉴가 없다"*)를 실측하니
> 패널 **289px** vs `voice-control-bar` 트랙 **226px(402×812) / 248(874) / 177(667)** —
> **모든 뷰포트에서 트랙을 넘고 부모의 `overflow:hidden`이 잘라내고 있었다.**
> ⚠️ 탭바 침범이 아니다(컨트롤바 bottom == 탭바 top: 723=723 · 785=785).
>
> **민구 처방 확정: ⓐ 트랙 안에서의 내부 스크롤.** 구현·검증 완료(앱 `2753798`).
> 3뷰포트 전부 컨트롤바 넘침 해소 + `elementFromPoint` 히트테스트로 행·`+`버튼 도달 확인.
> 정본 오라클: `tests/v0460-fb-b-drawer-reachable.spec.ts` (구 `⑦-c`는 삭제 — 틀린 질문이었다).
>
> 🔸 **남은 것은 하나뿐:** 2~3m 거리·장갑 낀 손에서 **스크롤 방식이 실제로 쓸 만한가** —
> 민구 실기기 판정 대기. 불편하면 ⓒ재배치·ⓓ요약필 승격으로 바꾼다(선택지는 아래 그대로 유효).

<details><summary>원문 (접힘 — 범위가 틀린 질문이지만 실측·선택지는 유효하다)</summary>

# ❓ Q4 🔴 BLOCKING — 375×667에서 서랍이 화면을 넘는다 (민구 A1 주의② 적중)

## 실측 (오라클 ⑦ · 명령 그대로)

```
SURVEY_BASE_URL=http://localhost:5183 npx playwright test tests/v046-chip-sweep.spec.ts --workers=1
drawer@375: panelH=289.0 rowBottom=689.8 vh=667 overflow=+22.8 collapsedH=42.0
drawer@402: panelH=289.0 rowBottom=826.5 vh=874 overflow=-47.5 collapsedH=42.0
collapsed@375: h=42.0 label="허용 인식률 60% · 안내속도 1.05x ⌄"
```

| 축 | 결과 |
|---|---|
| **375×667(최소 지원 규격)** | 🔴 칩 왕복 행 아래쪽이 **22.8px 화면 밖** |
| 402×874 | 🟢 47.5px 여유 |
| 접힌 토글 높이 | 🟢 **42.0px** — `heroLayout.ts:124`의 49px 계약 범위 안 (**WP-G 소유 계약 무간섭**) |
| 접힌 요약 문자열 | 🟢 불변 — 칩 왕복을 요약 필에 넣지 않았다(§C5-b 겹침 관례 보존) |

👉 **주의①은 통과, 주의②는 걸렸다.** 새 항목이 **펼친 높이만** 늘린 것은 맞다(접힌 상태 42.0px
불변). 다만 서랍 항목이 3개 → 4개가 되면서 펼친 높이 289px가 375 높이에서 안 들어간다.

## 후보 (고르지 않았다 — 민구가 볼 화면이다)

**안ㄱ — 서랍에 내부 스크롤**: 넘치면 서랍만 세로 스크롤. 항목이 더 늘어도 안전하다.
❌ 장갑 낀 손으로 작은 영역을 스크롤해야 하고, 아래 항목이 있다는 걸 모를 수 있다.

**안ㄴ — 칩 왕복을 2열 그리드의 반칸으로**: 말끊기 아래 빈 반칸에 넣어 1행을 절약.
❌ 375에서 3열(48+값+48)이 반칸에 들어가면 터치 타깃 48px이 깨진다(BargeInToggle이 전폭인
바로 그 이유 — `ActiveControlSteppers.tsx:172-176` 주석).

**안ㄷ — 말끊기와 칩 왕복을 한 행에 나란히**: 둘 다 토글/스텝퍼라 압축 여지가 있다.
❌ 같은 48px 타깃 문제.

**안ㄹ — 그대로 둔다**: 375는 최소 지원 규격이고 민구 실기기는 그보다 크다. 402에서는 47.5px
여유가 있다. ❌ 최소 지원 규격 계약(민구 확정 08-04)을 어긴다.

## 막힌 범위

🔴 **이 항목만.** 왕복 기능·설정 배선·오라클·게이트는 전부 완료했고 커밋했다(`e28806a`).
오라클 ⑦-c가 `test.fail()`로 이 넘침을 **명시적으로 표시**해 둔다 — 해소되면 그 테스트가
빨개져서 알려준다(조용히 통과하지 않는다).

---

# ✅ A4 — **말끊기 + 칩 왕복을 한 줄에 (2열)** (민구 선택 08-05)

민구가 세 안(2열 배치 / 서랍 내부 스크롤 / 간격 축소)을 보고 **2열 배치**를 골랐다.

```
지금 (4행)                    바꾸면 (3행)
┌허용 인식률┐ ┌ 안내속도 ┐    ┌허용 인식률┐ ┌ 안내속도 ┐
│ [−] 60% [+]│ │[−]1.05x[+]│    │ [−] 60% [+]│ │[−]1.05x[+]│
└───────────┘ └──────────┘    └───────────┘ └──────────┘
┌ 말끊기        켬     [✓] ┐   ┌  말끊기  ┐ ┌  칩 왕복  ┐
└──────────────────────────┘   │ 켬  [✓] │ │ 8초 [−][+]│
┌ 칩 왕복      8초   [−][+] ┐   └──────────┘ └──────────┘
└──────────────────────────┘
🔴 bottom 689.83 > 667          ✅ 한 행 통째로 절약
```

**왜 이 안인가(민구 판단 + 근거):** ①허용 인식률·안내속도가 **이미 2열**이라 시각적 일관성이
유지된다 ②22.83px보다 **훨씬 많이** 줄어 여유가 생긴다 ③**스크롤이 안 생긴다** —
장갑 낀 손·야외 사용에서 스크롤은 불리하다 ④**터치 타깃 크기를 안 줄인다**(간격 축소안의 위험).

## 🔴 확인할 것

1. **375×667에서 서랍 bottom ≤ 667**을 실측하고 **수치를 산출물에 적어라**. 여유(px)도 함께.
2. 🔴 **터치 타깃**: 2열로 좁아져도 각 컨트롤이 **44px 하한**(`PRINCIPLES §2` 장갑 조작)을
   지키는지 확인해라. 못 지키면 그건 새 BLOCKING이다 — 그때는 스크롤안으로 되돌린다.
3. **말끊기 토글이 2열에서도 읽히는지** — 라벨이 잘리면 안 된다(`expectChipLabelNotClipped`
   계열 판정을 서랍에도 적용할 수 있는지 보고, 안 되면 직접 재라).
4. 🔴 **오라클에 375×667을 넣어라.** 이 사건이 그 뷰포트에서만 드러났다 —
   **재발을 잡을 그물이 없으면 다음에 항목이 늘 때 그대로 다시 넘친다.**

## 🟢 A1 주의②가 적중한 것에 대해

내가 A1에서 *"서랍이 길어진다 … 375px에서 화면을 넘는지 확인하고 수치를 산출물에 적어라"* 로
넘긴 축을 **네가 실측으로 잡았고, 접힌 상태(42.0px, heroLayout 49px 계약 안)가 안전하다는 것까지
갈라서** 「펼친 높이만 늘었다」를 증명했다. **WP-G 계약 무간섭까지 확인한 판정**이라 그대로 채택한다.

## 🟢 그리고 8 failed 판정 — 절차가 정확했다

*"①72조합 스윕 단독 재실행 = 잘림 0건 ②v039 실패분 격리 재현(-g, workers=1) → 살아남은 4건이
전부 도트 계열이고 실패 메시지가 「180 기대 → 350 수신」 = WP-G 과제 그대로 ③격리에서 통과한
3건은 3스펙 동시 실행 부하 flake"* — **인과 격리 → 단독 재현 → 메시지 대조**의 3단이다.
🔑 이 레포가 `[TEAMOPS-70]`에서 **옳은 처방을 부하 시그니처 때문에 되돌린** 사고를 겪었다.
네 절차가 정확히 그 재발을 막았다. **칩존 회귀 0 판정을 채택한다.**

</details>
