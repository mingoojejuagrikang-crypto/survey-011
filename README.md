# survey-011

음성 입력 기반 현장 측정 기록 PWA. 이어폰을 끼고 양손이 자유롭지 않은 상태에서
TTS 안내와 음성 인식만으로 측정값을 Google Sheets에 기록합니다.

## 📱 접속 링크 (바로 사용)

> **👉 https://mingoojejuagrikang-crypto.github.io/survey-011/**
>
> - 스마트폰 **Chrome**으로 위 주소에 접속하세요.
> - 메뉴 → **홈 화면에 추가**를 누르면 앱처럼 설치됩니다.
> - **현재 버전: v0.40.0**

## 설치와 첫 사용

1. 스마트폰 Chrome에서 위 배포 URL에 접속합니다.
2. 메뉴 → **홈 화면에 추가**로 PWA를 설치합니다.
3. **설정 탭** → Google 로그인 → 스프레드시트 URL 붙여넣기
4. 컬럼 카드에서 각 항목의 **데이터형·입력방식**을 조정합니다.
5. `오늘 테이블 생성`을 누릅니다.
6. **입력 탭** → 이어폰 착용 → `음성 입력 시작`
7. TTS 안내를 듣고 값을 말하면 자동으로 다음 항목·행으로 진행합니다.

세로 화면 전용입니다(가로로 돌리면 "세로로 돌려주세요" 안내가 뜹니다).

## 기능

- **설정** — Google OAuth 로그인 → 스프레드시트 URL → 컬럼 자동 분석 → 데이터형/입력방식 설정 → 오늘 테이블 생성
- **입력** — 항상 켜진 마이크 + TTS 안내 + 자동 행 진행 + Wake Lock(화면 꺼짐 방지) + 이상치 추세 경보
- **데이터** — IndexedDB 영속화 + Google Sheets 자동 동기화 + CSV/ZIP 내보내기 + 음성 클립 재생
- **개선요청** — 화면 캡처와 로그를 첨부해 바로 보낼 수 있는 피드백 탭
- **오프라인 내성** — 네트워크·로그인이 끊겨도 기록과 경보는 계속 동작하고, 복구되면 큐가 올라갑니다

## 음성 명령

> **기능당 한 단어**입니다(v0.4.0부터 별칭 없음). 입력 화면의 **`？명령어`** 버튼이나
> **"도움말"** 발화로 전체 목록을 볼 수 있습니다.
> 아래 표의 SSOT는 [`src/lib/voiceCommands.ts`](./src/lib/voiceCommands.ts)입니다.

| 키워드 | 동작 |
|--------|------|
| 숫자 | 현재 항목에 입력 (한글 수사·아라비아 모두 지원) |
| `수정` (+ 값) | 직전에 입력한 값을 고침 (`178.1 수정`처럼 값 뒤에 붙여도 됨) |
| `이전` | 이전 행으로 이동 (완료된 행은 값을 읽어주고 대기) |
| `다음` | 다음 행으로 이동 (입력 중이던 행은 빈 행으로 남아 데이터 탭에서 채울 수 있음) |
| `취소` | 현재 인식된 값을 지움 |
| `유지` | 현재 항목의 값을 그대로 두고 다음으로 진행 |
| `확인` | 추세 경보에서 방금 입력한 값을 그대로 확정 |
| `일시정지` / `재시작` | 입력 멈춤 / 재개 |
| `종료` | 입력을 끝내고 저장 |
| `도움말` | 음성 명령어 도움말 열기 |
| `입력조절` | 허용 인식률·안내속도 조절판 열기/닫기 |
| `인식률낮추기` / `인식률높이기` | 허용 인식률 한 단계 조절 |
| `안내속도느리게` / `안내속도빠르게` | 음성 안내 속도 한 단계 조절 |

"이전행"·"다음행"처럼 뒤에 말을 붙여도 앞부분으로 인식됩니다.

한국어 수사 예시: `삼십오 점 일` → `35.1`, `일점오` → `1.5`, `이천이십육` → `2026`

## 문서 지도

| 문서 | 언제 읽나 |
|------|-----------|
| [CHANGELOG.md](./CHANGELOG.md) | 버전별로 무엇이 바뀌었는지 |
| [AGENTS.md](./AGENTS.md) | **AI·에이전트가 이 레포에서 작업을 시작할 때 (여기부터)** |
| [PRINCIPLES.md](./PRINCIPLES.md) | 설계·리뷰 판단 기준이 되는 앱 고유 원칙 |
| [CONTRIBUTING.md](./CONTRIBUTING.md) | 개발 환경·테스트·릴리스 절차 |
| [KNOWN-ISSUES.md](./KNOWN-ISSUES.md) | 지금 열려 있거나 관측 중인 문제 (관련 카테고리만 검색해서 읽기) |
| [ENGINEERING-GUARDRAILS.md](./ENGINEERING-GUARDRAILS.md) | 해결됐지만 **다시 어기면 안 되는** 계약 |
| [KNOWN-ISSUES-ARCHIVE.md](./KNOWN-ISSUES-ARCHIVE.md) | 종결된 사건 기록 (역사) |
| [docs/INDEX.md](./docs/INDEX.md) | 아키텍처·실기기 검증 등 상세 문서 색인 |

> 🛠️ **개발자/AI에게:** 코드를 만지기 전에 [`AGENTS.md`](./AGENTS.md)를 먼저 읽으세요.
> 새 함정을 만나면 [`KNOWN-ISSUES.md`](./KNOWN-ISSUES.md)에 같은 형식으로 추가해 주세요.

## 코드 구조 (개요)

```text
src/
├── components/     화면별 표현 컴포넌트 (voice/ settings/ data/ + 공용)
├── screens/        설정·음성입력·데이터 화면
├── stores/         Zustand 상태와 영속화 경계
├── lib/            음성·클립·인증·동기화 도메인 로직
└── styles/         전역 CSS·디자인 토큰
tests/              Playwright 회귀·E2E·실기기 근사 테스트
```

파일 단위 상세는 [docs/ARCHITECTURE.md](./docs/ARCHITECTURE.md)에 있습니다.

## 개발

```bash
npm install
cp .env.example .env.local        # VITE_GOOGLE_CLIENT_ID 설정
npm run dev                       # http://localhost:5173
```

테스트·릴리스·Google Cloud 설정을 포함한 전체 절차는
[CONTRIBUTING.md](./CONTRIBUTING.md)를 보세요.

---

> `growth-survey-010`을 독립 복제한 후속 라인. 원본과 분리되어 독자적으로 진행합니다.
