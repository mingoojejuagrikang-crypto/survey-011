# ❓ BLOCKING (wp-b) — 중앙의 **고정 상한**을 WP-B가 제거해도 되나

> 레인: `wp-b` · 올린 시각: 2026-08-05 · 상태: **답 대기 중 (해당 과제 중단)**
> 🔴 등급 판단 근거(브리핑 §6 표): **fit 계약 변경 · 남의 파일 · 되돌리기 비싼 결정** 세 항목에
> 동시에 걸린다. 애매하면 BLOCKING이라는 지시에 따라 올린다.

---

## 1. 무엇이 모호한가

**하한 오라클을 깔았고, 예정대로 red가 났다.** 그런데 red를 없애려면 손대야 할 파일이
**브리핑 §7의 「네 파일」에도, 「만지지 마라」 목록에도 없다.** 그리고 플랜 §6은 그 대상을
**"이번에 안 하는 것"** 으로 명시해 두었다.

### 🔴 실측 — "너무 작게"의 구조적 원인은 `clamp(…, N px)` 고정 상한이다

중앙에 뜨는 텍스트를 전수 조사한 결과, fit 배선 상태가 **두 갈래**로 갈린다.

| 대상 | 폰트 식 | fit 상태 |
|---|---|---|
| 실시간 인식값 (기준) | `max(24px, calc(44px * var(--fit-value,1)))` | 🟢 **열림** |
| 확정값 | `max(26px, calc(64px * var(--fit-value,1)))` | 🟢 열림 (같은 그룹) |
| 완료 요약 `X / N` | `max(24px, calc(40px * var(--fit-summary,1)))` | 🟢 열림 |
| 알람 비교 라벨·값 | `max(22px, calc(13.93vw * var(--fit-compare-label,1)))` 외 | 🟡 열렸으나 기준이 **vw** |
| 🔴 **알람 경보행** | `max(17px, calc(clamp(22px, …, **36px**) * var(--fit-lo,1)))` | 🔴 **36px 천장** |
| 🔴 **알람 중 인식값** | `max(24px, calc(clamp(44px, …, **96px**) * var(--fit-hi,1)))` | 🔴 **96px 천장** |
| 🔴 **완료 영수증** | `max(15px, calc(clamp(17px, …, **26px**) * var(--fit-lo,1)))` | 🔴 **26px 천장** |

🔑 게다가 이 셋이 곱하는 `--fit-lo` / `--fit-hi`는 **`useFitScale`(구 훅)이 내리는 축소 전용
배율이라 1을 넘지 않는다** — `heroLayout.ts:220-228` 주석이 *"장식이다 — 있다고 믿지 마라"* 라고
직접 적어 놓았다. 즉 **위로 여는 경로가 아예 없다.**

### 그런데 손댈 파일이 §7 목록 밖이다

| 고쳐야 할 곳 | §7에서 | 문제 |
|---|---|---|
| `heroLayout.ts:193` (`alarmLabel` 36px) | ⚠️ **허용에도 금지에도 없음** | 같은 파일에 `CHIP_TYPE`(**WP-D 레인**)과 `VOICE_TYPE.controlButton`(**WP-G 레인**)이 함께 산다 |
| `heroLayout.ts:214` (`alarmInterim` 96px) | ⚠️ 위와 같은 파일 | 위와 같음 |
| `CompleteSummary.tsx:87` (영수증 26px) | ⚠️ **허용에도 금지에도 없음** | — |

**그리고 플랜 §6 「이번에 안 하는 것」에 `고정 상한 4건(규칙 2 위반 부채)`이 명시돼 있다.**

---

## 2. 후보 — 두 안을 나란히 둔다 (🔴 내가 고르지 않는다)

### 안 (a) — WP-B가 중앙의 고정 상한을 지금 제거한다

- 민구 요구 *"중앙 **전부** 유동 크기로"* 를 이번 회차에 실제로 닫는다.
- `heroLayout.ts`의 `STATE_TYPE` 2건 + `CompleteSummary.tsx` 1건을 hero와 같은
  `max(하한, calc(기준px * var(--fit-*)))` 형태로 바꾸고 `useFitGroup`에 배선한다.
- ⚠️ 비용: `heroLayout.ts`를 WP-D·WP-G와 **동시 편집**하게 된다(커밋 충돌 위험).
- ⚠️ 비용: 플랜 §6의 "안 하는 것" 결정을 이번 회차에 뒤집는 것이다.

### 안 (b) — WP-B는 **하한 오라클만** 남기고 상한 제거는 「고정 상한 4건」 부채로 넘긴다

- 브리핑 §11이 *"이 과제는 「오라클만 깔고 수정은 다음 세션」도 완전히 유효한 결과"* 라고
  미리 허락한 경로다.
- 남는 것: red 16건이 **의도된 red**로 스위트에 남는다(태그 필요 — `@pending-wpb-ceiling` 등).
- ⚠️ 비용: 민구가 제보한 증상이 이번 배포에서 **안 고쳐진다.** 재전달된 요구라 4번째 회차가 된다.

---

## 3. 왜 혼자 못 정하나

1. 🔴 **남의 파일이다.** `heroLayout.ts`는 WP-D(`CHIP_TYPE`)·WP-G(`VOICE_TYPE.controlButton`)가
   같은 회차에 만질 수 있는 파일이고, 세 레인이 같은 트리에서 동시에 돈다(§7). 내가 임의로
   열면 커밋이 서로 덮인다.
2. 🔴 **플랜이 명시적으로 제외한 항목이다.** §6이 "안 하는 것"에 적어 둔 것을 내 판단으로
   되살리는 것은 **되돌리기 비싼 결정**이다.
3. 🔴 **fit 계약 변경이다.** `--fit-lo`/`--fit-hi`(구 훅 `useFitScale`, 축소 전용)를 쓰는 슬롯을
   `useFitGroup`(신 훅, 열린 이진탐색)으로 옮기는 것은 두 fit 기제의 **경계를 옮기는** 일이다.
   `v043-fit-group.spec.ts`의 *"fit 기제 경계"* 오라클이 그 경계를 지키고 있다.

🔴 **브리핑 §6 마지막 줄 준수:** *"혼자 선택지 표를 만들어 하나를 고르지 마라."* — 고르지 않았다.

---

## 4. 막힌 범위 / 막히지 않은 범위

**🔴 막힘 (답까지 중단):**
- 제품 코드 수정 전부 — `heroLayout.ts` · `CompleteSummary.tsx` · `CenterStage.tsx` ·
  `AnomalyAlertPopup.tsx`의 폰트·fit 배선 변경.
- 게이트 §5-2(72조합 스윕)·§5-4(정당 파손 전수 grep) — 제품 변경이 있어야 의미가 있다.

**🟢 안 막힘 (계속 진행 중):**
- 하한 오라클 자체의 정확도 개선 (폭/높이 신호 분리 · 빈 요소 가드 · 픽스처 수정).
- §3-1 블래스트 반경 재측정 — **제품 코드를 안 건드리고** `page.evaluate`로 CSS 강제를
  개별 무력화해 여유 델타를 재는 방식으로 진행한다.
- 산출물 작성.

---

## 5. 🔴 함께 보고 — 브리핑 전제 2건이 실측과 어긋난다 (§0-2)

1. **`CenterStage.tsx:53`의 `line-height: 1 !important`는 이미 없다.**
   브리핑 §3-1과 플랜 §3-1이 *"현존하며 그 한 줄만 빼는 처방은 실패해 되돌렸다"* 로 전제하나,
   `grep` 결과 **CSS 선언 0건**(주석 언급 1건뿐, `:19`). 커밋 `488adbc`
   *"free the alarm compare fit from its floor (§C0)"* 가 제거했고 **되돌려지지 않았다.**
   `CenterStage.tsx:53`은 현재 `anomaly-next-label`의 `grid-row` 블록이다.
   👉 §3-1 "블래스트 반경 재측정"의 **대상이 바뀐다** — 남은 강제는
   `row-gap:0` · `overflow:hidden` · `padding-block:0` 셋이다.
2. **브리핑 §7의 `src/lib/fitGroup.ts`는 실재하지 않는다** — 실제 경로는
   `src/components/voice/fitGroup.ts`. 파일 동일성이 명확해 경로만 정정하고 진행했다.
3. **브리핑 §8이 참조한 `TODO.md`가 레포에 없다** — 「긴 항목명」 감사 항목의 원문을 확인하지
   못했다. 오라클 픽스처에는 지시대로 「긴 항목명 + 긴 값」 케이스를 **넣었다**(`과실종경측정값` /
   `1234.56`).

---

## 6. 참고 — 하한 오라클은 이미 서 있고 red를 냈다

`tests/v0460-fit-headroom.spec.ts` (신설). 직렬 단독 실행 `--workers=1`, 26 테스트 11.2분.

```
22 failed / 4 passed
```

| 구분 | 결과 | 해석 |
|---|---|---|
| 기준① 실시간 인식값 | 🟢 **4/4 green** | **오라클이 옳다는 증거** — 민구가 지목한 "잘 되는 경로"는 여유가 없다 |
| 🔴 대상①②③④ 알람 4종 | 🔴 **16/16 red** | **찾던 것.** 폰트를 ×1.15 해도 폭·높이 **둘 다** 안 넘친다 |
| 기준② 확정값 | ❌ 4/4 실패 | **테스트 결함**(셀렉터/타이밍) — 수정 중 |
| 대상⑤ 완료 요약 | ❌ 2/2 실패 | **픽스처 결함**(끝 도달 미진입) — 수정 중 |

🔑 **이 red는 오탐이 아니다.** 브리핑 §3-5가 경고한 `scrollHeight` 역방향 오탐은 높이를
**실제보다 크게** 재므로 `hasHeadroom=false` → **green 방향**으로만 작용한다. red는
폭·높이가 **둘 다** 안 넘쳤을 때만 나오므로 그 오탐 경로로는 발생할 수 없다.

---

# ✅ A — **안 (a) 확정: 중앙 3건의 고정 상한을 지금 제거한다** (민구 선택 08-05)

민구가 세 선택지(중앙 3건만 / 오라클만 남기고 다음 회차 / 상한 5건 전부)를 보고
**「중앙 3건만 지금 풀기」** 를 골랐다.

## 🔑 네 조사가 이 결정을 만들었다

*"`--fit-lo`/`--fit-hi`는 축소 전용이라 1을 넘지 않는다 → **위로 여는 경로가 아예 없다**"* —
이 한 줄이 «상한을 안 풀면 WP-B는 아무것도 못 고친다»를 증명했다. **하한 오라클이 red를 냈고
(16/16), 그 red의 원인이 계수가 아니라 구조라는 것**까지 갈랐다. `[TEAMOPS-58]` 그대로다 —
*"「해가 없다」가 나오면 계수가 아니라 구조를 의심한다."*

## 🔴 파일 소유 확장 — 아래 3곳이 **네 것**이 된다

| 파일·위치 | 허용 범위 |
|---|---|
| `src/lib/heroLayout.ts:193` `alarmLabel`(36px) | 🟢 **이것만** |
| `src/lib/heroLayout.ts:214` `alarmInterim`(96px) | 🟢 **이것만** |
| `src/components/voice/CompleteSummary.tsx:87` 영수증(26px) | 🟢 파일 전체 |

🔴 **`heroLayout.ts`의 나머지는 계속 남의 것이다** — `CHIP_TYPE`(WP-D) · `VOICE_TYPE.controlButton`
(WP-G)을 **건드리지 마라.** 같은 파일을 만지되 **다른 상수만** 만진다.
🟢 **충돌 위험은 실측으로 0이다** — Larry가 확인했다: `git status src/lib/heroLayout.ts
src/components/voice/CompleteSummary.tsx` → **아무 레인도 두 파일을 만지지 않았다.**
⚠️ 그래도 **커밋 직전에 다시 확인해라**(`git status` 그 순간 값). 오늘 그걸 안 해서 사고가 한 번 났다.

## 🟢 플랜 §6 예외의 근거 — 임의 예외가 아니다

플랜 §6이 「고정 상한 4건」을 *"이번에 안 하는 것"* 으로 적은 것은 맞다. **다만 그 보류에는
선행 조건이 있었고, 그 조건이 이미 충족됐다:**

> `TODO.md:123` — *"**왜 `UI-g`에 안 묶었나(민구 승인 08-02)**: ①상한 제거는 **값이 변해**
> 정당 파손 판정 절차가 붙는다 ②**검사기를 먼저 세워야** 상한 제거 후 재발을 막는다."*
> `TODO.md:129` — *"내용 기반이라 **상한을 없애면 검사기가 자동으로 알려준다.** 그때 해당 줄을 지운다."*

🔑 **그 검사기는 `UI-g`가 이미 만들었다**(HANDOFF: *"검사기가 생겼으니 이제 착수 가능하다"*).
즉 보류 사유 ②는 해소됐고, ①(정당 파손 판정)은 **네가 밟을 절차**다.
👉 **`UI-g` 검사기가 상한 제거를 자동 검출하면 해당 예외 줄을 지워라** — TODO가 지시하는 그 동작이다.

## 🔴 반드시 밟을 것 3가지

1. **`v043-fit-group.spec.ts`의 「fit 기제 경계」 오라클** — 구 훅(`useFitScale`)에서 신 훅
   (`useFitGroup`)으로 슬롯을 옮기면 **그 경계가 이동한다.** red가 나면 **정당 파손인지
   회귀인지 갈라서** 판정하고, 정당 파손이면 경계 정의를 갱신하되 **무엇이 어디로 옮겨갔는지**
   주석에 남겨라. (`[TEAMOPS-64]` — 이 스펙은 `page.evaluate` 직렬화 계약이 걸린 곳이다.
   **`fitGroup.ts`의 판정 함수를 「자기 완결」에서 벗어나게 만들지 마라.**)
2. **상한 제거는 값이 변한다 → 정당 파손이 여러 건 난다.** 각 건을 「계약이 바뀐 것」으로
   판정하고 **바뀐 계약을 주석에 적어라**(`36px 천장` → `화면 배분이 정한다`).
3. **하한 오라클 16건 red → green**이 이 과제의 완료 판정이다. 🔴 **동시에 잘림 오라클
   (72조합 스윕 + `expectChipLabelNotClipped`)이 회귀 0이어야 한다** — 위로 열면 넘칠 수 있다.
   **두 축을 함께 봐라.** 하나만 green인 상태는 완료가 아니다.

## ⚠️ 안 (b)로 후퇴하는 조건 (미리 정한다)

**2라운드 안에 「하한 green + 잘림 회귀 0」을 못 만들면 안 (b)로 후퇴한다**(플랜 §4-3 트리거).
그때는 상한 제거를 되돌리고 **하한 오라클만 `@pending-wpb-ceiling` 태그로 남긴다.**
🔴 **후퇴는 실패가 아니다.** 되돌린 이유를 산출물에 적으면 다음 회차가 거기서 시작한다.
막히면 **혼자 계수를 돌리지 말고 BLOCKING으로 올려라** — §C1에서 그렇게 두 회차를 썼다.
