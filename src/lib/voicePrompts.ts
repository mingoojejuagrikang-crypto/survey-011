/** v0.36.0 FB#4(Vance) — 음성 안내(TTS)와 화면 재질문 큐가 공유하는 프롬프트의 SSOT.
 *  소수점 유실 재질문 문구가 종전엔 세 콜사이트에 인라인 리터럴로 흩어져 있어(useVoiceSession)
 *  TTS로만 나가고 화면엔 없었다. 여기 한 곳으로 모아 say()와 ReaskCue가 같은 상수를 공유한다.
 *
 * ─────────────────────────────────────────────────────────────────────────────────
 * 🔴 **v0.49 r2 W2 계약 개정(민구 2026-08-13) — TTS본과 화면본은 더 이상 같지 않다.**
 *
 * 민구 원문: *"이번에 TTS와 화면 문구는 일치하지 않을거야. 음성안내를 가능하면 축약해서 업무
 * 속도를 올리고 싶거든."*
 *
 * 배경: 이 앱의 사용자는 폰을 2~3m 떨어뜨려 놓고 장갑 낀 손으로 일한다 — **화면은 자주 못 보고
 * 귀만 열려 있다.** 그래서 긴 TTS는 그대로 대기 시간이고, [TTS-WATCHDOG-1]에서 긴 발화일수록
 * 절단률이 단조 증가하기까지 한다. 반면 화면은 눈이 갔을 때 정보가 많을수록 낫다.
 *
 * 그래서 PRINCIPLES §2는 「글자까지 일치」가 아니라 **「의미 동등 + 구조적 분리」**다:
 *   · **의미 동등** — 두 표면이 같은 사실을 말한다. 한쪽만 아는 상태가 있으면 안 된다.
 *   · **구조적 분리** — 분리는 **이 파일의 쌍 상수로만** 한다. 콜사이트에 인라인 리터럴을
 *     흩뿌리는 방식으로 갈라놓으면 v0.36.0 이전으로 되돌아간다(그때 화면과 TTS가 조용히 어긋났다).
 *
 * 이름 규칙: `*_TTS` = 귀로 나가는 축약본 · `*_SCREEN` = 눈으로 보는 상세본.
 * ───────────────────────────────────────────────────────────────────────────────── */

/** 소수부 타깃 재질문 — **TTS·화면 공용**(이 문구는 개정 대상이 아니다, 확정표 #3 「현행 유지」).
 *  분리하지 않은 이유: 축약할 여지가 없고, 정수부 문맥을 양쪽이 똑같이 말해야 한다. */
export function decimalReaskPrompt(whole: string | number): string {
  return `${whole} 점, 소수점 아래 숫자만 말씀해 주세요.`;
}

/** 재질문 사유 2버킷 — **화면본**(`ReaskCue`). v0.48.0 P3 이후로 TTS도 이걸 읽었으나,
 *  W2 개정으로 TTS는 아래 `REASK_TTS`로 갈라졌다. 화면 문구 자체는 **불변**이다. */
export const REASK_SCREEN: Record<'low_confidence' | 'parse_failed', string> = {
  low_confidence: '소리가 불확실',
  parse_failed: '숫자로 인식 실패',
};

/** 재질문 사유 2버킷 — **TTS본**(확정표 #1·#2, 민구 08-13 확정. 바이트 그대로).
 *
 *  🔴 **꼬리를 되살리지 마라.** 종전엔 `"{사유}. 잘 못 들었습니다. {항목} 다시 말씀해 주세요."`
 *  였다. 재질문 지시("다시 말씀해 주세요")가 사라진 자리는 **부정 비프 + 화면 `ReaskCue`**가
 *  전담한다 — 사용자는 이미 그 셀에 답하는 중이고, 무엇을 하라는지는 비프가 말한다.
 *  화면 상세는 `REASK_SCREEN`이 그대로 들고 있다(의미 동등).
 *
 *  민구 08-13: 편입 4분기(컬럼명·잡음어·응답어·단음절)도 같은 문구로 통일 승인. */
export const REASK_TTS: Record<'low_confidence' | 'parse_failed', string> = {
  low_confidence: '소리가 불확실.',
  parse_failed: '숫자로 인식 실패.',
};

/**
 * 🔴 v0.49 r4 M11(민구 D2 08-13 · codex r3 F8) — **같은 셀을 다시 듣겠다**는 재청취 안내.
 *
 * 이 한 문장이 네 콜사이트에 **인라인 리터럴**로 복제돼 있었다(`useVoiceSession`의
 * `cmdModify` 재수정 · `cmdCancel` · `modifyAnomalyTouch` · 저신뢰 명령 거절).
 * §2가 금지하는 형태 그대로다 — *"구조적 분리는 **이 파일의 쌍 상수로만**"*. 사본이 넷이면
 * 확정표가 다음에 이 문구를 손볼 때 **어느 하나가 조용히 남는다**([PAST-2] 형태).
 *
 * ⚠️ **바이트는 그대로 옮긴다.** W2가 지운 것은 「거절 사유 뒤에 붙던 꼬리」이고, 이 문장은
 * 거절이 아니라 **재청취 국면의 안내**다(사용자 입력은 접수됐고 앱이 같은 칸을 다시 듣는다).
 * 두 상황을 같은 개정으로 묶지 않는다.
 *
 * 화면본을 따로 두지 않는 이유: 이 국면의 화면은 수정 표시(`ModifyIndicatorPill`)와
 * 활성 칩이 이미 「이 칸을 다시 받는 중」을 그린다 — 같은 말을 문장으로 또 쓰지 않는다.
 */
export function relistenPrompt(name: string): string {
  return `${name} 다시 말씀해 주세요.`;
}

/** 완료 행 검토 대기(reviewWait)에서 bare 값을 흡수했을 때의 안내 — **TTS본**(확정표 #4).
 *  종전: `"{N}행은 완료된 행입니다. 수정하려면 '수정', 다음 행은 '다음행'이라고 말씀해 주세요."`
 *  두 진입로('수정'·'다음행')를 **어휘만** 남겨 절반 이하로 줄였다 — 그 두 단어가 실제로 먹히는
 *  명령이라는 점이 이 문구의 핵심이고(fix49b #8), 설명은 도움말이 맡는다. */
/** 🔴 v0.49 r6 Y5(claude #4 · codex R5-F3) — 검토 대기에서 **실제로 먹히는 조작 어휘**.
 *  흡수 안내와 저신뢰 **명령** 거절의 꼬리가 이 하나를 공유한다. 두 표면이 같은 상태를
 *  가리키므로 문구도 하나여야 한다(cellWaitPrompt가 세운 *"같은 상태에 두 이름을 주지 않는다"*
 *  와 같은 계약). 종전 거절 꼬리는 `relistenPrompt`(= "{항목} 다시 말씀해 주세요")여서
 *  **이 상태에서 받을 수 없는 값**을 요구했다 — 시킨 대로 하면 흡수 안내가 되받는다.
 *  ⚠️ 늘리지 마라([TTS-WATCHDOG-1] 긴 발화일수록 절단률이 단조 증가). */
export const REVIEW_WAIT_COMMANDS_TTS = '수정 또는 다음행.';

/** 🔴 v0.49 fix49b(max 리뷰 #9) — **셀 검토 대기가 값을 요구하지 않는다는 것의 SSOT.**
 *
 *  이 상태는 bare 값을 **흡수**한다(덮어쓰기 금지 — B-1). 그런데 값 대기(`kind:'value'`)
 *  문맥에서 쓰던 재질문 문구("○○ 다시 말씀해 주세요")가 이 상태에서도 그대로 나갔다:
 *  앱이 값을 말하라고 시키고, 시킨 대로 하면 흡수가 "수정이라고 말하세요"로 되받는다 —
 *  서로를 부정하는 두 문장 사이에 음성 전용 사용자가 갇힌다.
 *  화면을 끄고 2~3m 떨어져 쓰는 사용자에게 TTS는 **유일한 조작 설명서**다(v0.47.0 V-FIX4).
 *
 *  흡수 안내(useVoiceSession의 cellWait 흡수)와 **같은 문구**를 쓴다 — 같은 상태에 두 이름을
 *  주지 않는다.
 *  ⚠️ 늘리지 마라([TTS-WATCHDOG-1] 긴 발화일수록 절단률이 단조 증가).
 *
 *  🔴 [ENV-12] E단계 E1(2026-08-15) — 종전 선언 위치는 `useVoiceSession.handleFinal` **안**
 *  (블록 B)이었다. 그 함수를 스테이지 훅으로 가르면 이 문구를 소비하는 세 지점이 **파일 경계를
 *  가로지른다**(명령 스테이지의 `cmdConfirm`·`cmdCancel`·저신뢰 명령 거절 꼬리 → `useFinalCommands`,
 *  cellWait 흡수 → 본체 잔류). 훅 간 주입 심볼로 넘기면 같은 문장의 소유자가 흐려지므로,
 *  이 파일(문구 SSOT)로 올린다 — 위 `REVIEW_WAIT_COMMANDS_TTS` 헤더가 이미 이 함수를 「같은
 *  상태에 두 이름을 주지 않는다」 계약의 전례로 인용하고 있었다. **바이트는 그대로 옮겼다.** */
export function cellWaitPrompt(name: string): string {
  return `${name} 기록값입니다. 수정이라고 말하세요.`;
}

export function reviewWaitAbsorbTts(row: number): string {
  return `${row}행 완료됨. ${REVIEW_WAIT_COMMANDS_TTS}`;
}

/** 끝 도달 안내 — **TTS본**(확정표 #5+6 통합). 종전엔 트리거 2개가 **서로 다른 문구**를 썼다:
 *  ⓐ `announceEndReached` "마지막 행까지 입력했습니다… 종료하려면 '종료'라고 말씀하거나…"
 *  ⓑ atEnd 값 흡수 "입력이 끝났습니다. 종료하려면…"
 *  같은 상태를 두 이름으로 부르면 사용자는 두 상태가 있는 줄 안다 — 하나로 합치고, 대신
 *  **이번 세션에 몇 행을 끝냈는지**를 말한다(민구 확정: 종료 안내 꼬리보다 그 숫자가 쓸모 있다.
 *  종료 수단은 화면 하단 ⏹과 '종료' 명령으로 이미 상시 노출된다).
 *
 *  @param completedRows 완료 행 수 — **화면 `X / N`의 X와 같은 값**(`completedRows.length`)을
 *    넘겨라. 두 표면이 다른 숫자를 말하면 그게 §2 위반이다.
 *  @param emptyRowsPhrase `formatRowList(빈 행들)` 결과. 빈 행이 없으면 null — 꼬리 자체가 빠진다. */
export function endReachedTts(completedRows: number, emptyRowsPhrase: string | null): string {
  const head = `마지막행 입력. 이번 세션에 완료된 행은 ${completedRows}행.`;
  return emptyRowsPhrase ? `${head} ${emptyRowsPhrase}이 비어 있습니다.` : head;
}

// ─── v0.50 [CLIP-SILENT-1] 클립 캡처 실패 고지 ────────────────────────────────

/** 클립이 연속으로 비어 나올 때의 고지 — **TTS본**(축약).
 *
 *  🔑 **왜 귀로 말하는가**: 2026-08-19 이원창 세션에서 민구는 세션 시작 14초 뒤 화면 끄기를
 *  켰고, 그 뒤 40분간 60개 클립이 전부 죽는 동안 **어떤 시각 표면도 도달할 수 없었다**
 *  (`BlackoutOverlay` z-9999 > `MicReconnectBanner` z-60). 같은 날 양혁진 세션은 화면 끄기를
 *  **쓰지 않았는데도** 몰랐다 — 애초에 띄울 표면이 없었기 때문이다.
 *  👉 이 앱의 사용자는 폰을 2~3m 떨어뜨려 두고 장갑을 낀 채 일한다(PRINCIPLES §2).
 *  **눈이 아니라 귀가 유일하게 열려 있는 채널**이다. 조사 중 말이 끼어드는 대가는 민구가
 *  알고 고른 것이다(2026-08-19 결정).
 *
 *  세션당 1회만 나간다(`useClipFailureAlert`의 상승 에지 가드) — 반복 고지는 그 자체가 방해다. */
export const CLIP_FAIL_ALERT_TTS = '음성 클립이 저장되지 않고 있습니다. 마이크를 재연결해 주세요.';

/** 같은 사실의 **화면본**(상세) — `MicReconnectBanner`가 이미 「클립 녹음 끊김」 + 재연결 버튼을
 *  띄운다. 여기서는 종료 화면 결산에 쓰는 문구를 소유한다(§2 「의미 동등 + 구조적 분리」:
 *  두 표면이 같은 사실을 말하되 화면 쪽이 상세하다).
 *  @param failed 이번 세션에서 저장에 실패한 클립 수 @param total 시도한 클립 수 */
export function clipFailSummaryScreen(failed: number, total: number): string {
  return `이번 세션의 음성 클립 ${total}건 중 ${failed}건이 저장되지 않았습니다. 값은 정상 기록됐지만 음성 확인은 할 수 없습니다.`;
}
