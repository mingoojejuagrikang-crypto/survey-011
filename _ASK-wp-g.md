# 🔴 BLOCKING — WP-G 질문 2건 (레인 `wp-g`)

> 브리핑 `_BRIEF-wp-g.md` §0-2 *"브리핑 전제가 실측과 어긋나면 실측이 이긴다"* 에 따라
> **조용히 맞추지 않고 멈춘다.** 두 건 다 답이 올 때까지 코드를 만지지 않는다.
> 실측 명령·수치는 §Q1-근거 / §Q2-근거에 원문 그대로 있다.

---

## Q1 🔴 BLOCKING — **「도트 밀도 2배」는 이미 되어 있다**

### 무엇이 모호한가

브리핑 §3 **G-2 「도트 밀도 2배(민구 희망)」** 를 새 과제로 받았는데,
**같은 이름의 작업이 v0.44.0에서 이미 끝나 있고 민구가 그 화면을 이미 봤다.**

`src/components/voice/StateDots.tsx:36-40` 주석 원문:

> *"격자 치수 — v0.44.0 §C5(F14, 민구 확정 §4-a 9): 13×7 → **18×10**(셀 91 → 180 ≈ 2.0배).
> 민구 원문 **「밀도를 2배로」**."*

- 커밋: `0545fa3 feat(voice): double the dot field, idle wave, reclaim the bottom row (§C5·§C5-b)`
- 그 뒤 `59e0b59 release: v0.45.0` 이 나갔으므로 **민구는 18×10(180셀)을 이미 본 상태다.**
- 오라클에도 **리터럴로 박혀 있다** — `tests/v0440-c5-dots.spec.ts:5`
  *"② 밀도 2배 — 격자 18×10 = 180셀 리터럴"*, `:32-34` `expect(cells).toBe(180)`.

즉 이번 요청이 **①「180 → 360으로 한 번 더 2배」** 인지,
**②「이미 반영된 걸 모르고 같은 요청을 반복한 것」** 인지 판단할 근거가 없다.

### 후보 (고르지 않았다)

| | **안 A — 이미 완료로 종결** | **안 B — 180 → 360 재2배** |
|---|---|---|
| 격자 | **18×10 = 180셀 유지** | **25×14 = 350셀**(각 축 ×√2, 13×7→18×10과 같은 방식) |
| 도트 지름 | 12px 유지(실측) | 12px → **8.7px** (셀 17px→12.1px) |
| `v0440-c5-dots.spec.ts` | 그대로 green | **180 리터럴 3곳을 고쳐야 한다**(정당 파손) |
| 켜진 셀 비율 | 현행 유지 | 글리프 4종 전부 재작도 → 비율 재산출 필요 |
| 야외 판독성 | 현행 | 🔴 **2~3m 거리에서 도트가 28% 작아진다** — 미검증 |
| Q2와의 관계 | Q2를 독립으로 푼다 | Q2 안 B의 「행 예약」 비용이 상쇄된다(아래) |

### 왜 혼자 못 정하나

- 되돌리기가 비싸다 — 글리프 4종 재작도 + 오라클 리터럴 수정 + 발광 면적 재산출이 딸린다.
- **민구가 볼 화면**이고 브리핑 §5는 *"모양이 크게 달라지면 물어라"* 로 이 축을 BLOCKING으로 지정했다.
- 도트가 8.7px로 작아지는 건 이 앱의 1순위 제약(**폰을 2~3m 밖에 두고 장갑 낀 손으로 본다**)에
  정면으로 걸리는데, **판독성은 실기기 없이 판정할 수 없다.**

---

## Q2 🔴 BLOCKING — **브리핑 §3 G-1의 처방이 실측과 어긋난다**

### 무엇이 모호한가

브리핑 §3 G-1은 *"맨 아래 행 가운데 몇 셀을 **글리프가 쓰지 않게** 해서 접힌 필이 빈 자리에
앉게 한다"* 이고, §2는 *"원인은 이미 규명됐다 — 다시 조사하지 마라"* 다.
**원인 규명은 맞다.** 그런데 **처방의 대상이 실측과 다르다.**

🔑 **필은 `mode === 'nav'` 에서만 뜬다**(`ActiveState.tsx:252` `edgeMode`, `ActiveControlBar.tsx:59`
`controlsExpandable = mode === 'nav'`). 그래서 **`pause`·`alert` 글리프가 뜨는 상태에서는 필이
`display:none` 이라 애초에 겹치지 않는다.** 겹치는 것은 **글리프가 아니라 idle 웨이브·파형**이다.

**상태별 실측 (402×874, 켜진 셀 = computed opacity > 0.3):**

| 상태 | glyph | mode | 필 보임 | 켜진 셀 | **겹치는 켜진 셀** | 겹친 행 |
|---|---|---|---|---|---|---|
| nav 무음 | mic | idle | ✅ | 88 | **6** | 7 |
| nav 발화 | mic | wave | ✅ | 100 | **14** | 7, 8 |
| 일시정지 | pause | glyph | ❌ | 60 | **0** | — |
| 이상치 | alert | glyph | ❌ | 27 | **0** | — |
| 완료 | check | glyph | ✅ | 11 | **2** | 7 |

👉 **글리프 비트맵을 다시 그려서 없앨 수 있는 겹침은 `check`의 2셀이 전부다.**
나머지 20셀은 `litFromAmps()`(`StateDots.tsx:176`)가 매 프레임 만든다 — 비트맵에 없다.

**그리고 필이 덮는 면적 자체가 「가운데 몇 셀」이 아니다:**
필 `201×42px` vs 격자 `268×170px` → **격자 셀 42개(행 7·8·9 × 열 2~15)가 필 사각형 안에 있다
= 180셀의 23.3%, 가로로는 18열 중 14열.**

```
현재 18×10 격자 (● = idle 무음에서 켜진 셀, ▒ = 필이 덮는 셀 자리)

행0  · · · · · · · · · · · · · · · · · ·
행1  · · · · · · · · · · · · · · · · · ·
행2  · · ● · ● · · ● · ● · · ● · ● · · ·
행3  · ● ● ● · ● ● ● ● · ● ● ● · ● ● ● ●
행4  · ● ● ● ● ● ● ● ● ● ● ● ● ● ● ● ● ●
행5  · ● ● ● ● ● ● ● ● ● ● ● ● ● ● ● ● ●
행6  · ● ● ● · ● ● ● ● · ● ● ● · ● ● ● ●
행7  · ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ · ·   ← 여기부터 필. 이 행에 켜진 도트 6개가 깔린다
행8  · ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ · ·      필 = "허용 인식률 60% · 안내속도 1.05x ⌄"
행9  · ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ ▒ · ·
```

### 후보 (고르지 않았다)

두 안 모두 **24% 비율 계약(`v0440-zone-ratios.spec.ts` 층2b)과 오탭 안전을 유지**한다.
브리핑이 기각한 ②(필을 작게)·③(플로우 복귀)은 후보에 넣지 않았다.

#### 안 A — **격자 박스를 세로로 줄인다** (필 위에서 격자가 끝난다)

```
┌─ 도트행 = pool의 24% (170px, 불변) ─────────┐
│  ● ● ● ● ● ● ● ● ● ● ● ● ● ● ● ●   격자 120px │   10행 그대로
│  ● ● ● ● ● ● ● ● ● ● ● ● ● ● ● ●   셀 12px    │   파형 진폭 상한 4 그대로
│                                              │
│      ╔══════════════════════════╗            │
│      ║ 허용 인식률 60% · 1.05x ⌄ ║  필 42px    │   겹침 0
│      ╚══════════════════════════╝            │
└──────────────────────────────────────────────┘
```
- 바꾸는 곳: `StateDots.tsx`의 `size`/`gridHeight` 산출 + 밴드 정렬(상단 정렬)
- 🟢 파형·글리프의 세로 해상도 10행을 **그대로** 쓴다. 비트맵 재작도 불필요
- 🔴 **도트 지름 12px → 8.6px** (셀 17px → 12px). 야외 판독성 저하 — 실기기 미검증
- 🟡 `v0440-c5-dots.spec.ts`의 180셀 리터럴은 안 깨진다(행·열 불변)

#### 안 B — **하단 3행을 「필 자리」로 예약한다** (격자 박스·도트 크기 유지)

```
┌─ 도트행 = pool의 24% (170px, 불변) ─────────┐
│  ● ● ● ● ● ● ● ● ● ● ● ● ● ● ● ●   행0~6만   │   도트 12px 유지 ✅
│  ● ● ● ● ● ● ● ● ● ● ● ● ● ● ● ●   사용      │   세로 해상도 10행 → 7행 🔴
│                                              │
│      ╔══════════════════════════╗            │
│      ║ 허용 인식률 60% · 1.05x ⌄ ║  행7·8·9    │   겹침 0
│      ╚══════════════════════════╝            │
└──────────────────────────────────────────────┘
```
- 바꾸는 곳: `litFromAmps()`의 중심 `mid`를 4.5 → 3, 진폭 상한 4 → 3 · `check` 글리프 2셀 위로
- 🟢 **도트 지름 12px 그대로** — 야외 판독성 무변화
- 🔴 파형 세로 해상도가 10행 → 7행. §C5의 *"진폭 상한 4 = 전 10행"* 계약을 고쳐야 한다
- 🔴 **격자 180셀 중 42셀(23%)이 영구히 죽는다** — 「밀도 2배」의 취지와 반대 방향

#### 🔑 Q1 안 B(재2배)를 고르면 Q2 안 B의 대가가 사라진다

25×**14**행으로 늘리고 하단 4행을 예약하면 **실사용 10행** — 지금과 같은 세로 해상도에
겹침 0이다. 도트는 8.7px로 작아진다(= Q1 안 B의 대가와 동일, 중복 지불 없음).
**두 과제를 묶으라는 브리핑 §3의 결론은 맞다. 다만 근거(「어차피 글리프를 다시 그린다」)는
실측상 틀렸다** — 글리프 재작도로는 겹침의 2/22셀밖에 못 없앤다.

### 왜 혼자 못 정하나

- 브리핑이 지정한 처방(글리프 비트맵)으로는 **문제가 안 풀린다.** 다른 처방을 고르는 건
  §5의 *"되돌리기 비싼 결정 · 계약 충돌"* 에 해당한다.
- 두 안 다 **기존 계약을 하나씩 건드린다** — 안 A는 도트 지름(야외 판독성), 안 B는 §C5의
  진폭 상한 4(파형 표현력). 어느 쪽을 내줄지는 **민구가 화면을 보고 정할 축**이다.
- 판독성·파형 표현력 모두 **데스크톱 Playwright로 판정할 수 없다**(CLAUDE.md 계약 4항).

---

## 막힌 범위

- **G-1(겹침 해소)·G-2(밀도) 둘 다 코드 수정 착수 불가.** 답을 기다린다.
- **막히지 않은 것 — 답을 기다리는 동안 진행한다:**
  - §4-2 **겹침 오라클 신설** — 재는 축이 두 안 어느 쪽에서도 같다(필 사각형 ∩ 켜진 셀 = 0).
    현재 코드에서는 **의도된 red**가 된다(nav-idle 6셀 · nav-wave 14셀 · endReached 2셀).
    🔴 다른 레인의 회귀 판정을 오염시키지 않도록, **답이 오기 전까지는 스킵 태그를 달아 둔다.**
  - §4-3 정당 파손 전수 grep · §10 시트 불특정 원칙 점검 — 완료(산출물에 기록).

## 참고 — §10(시트 불특정) 점검 결과 🟢

`StateDots.tsx` 전문을 읽었다. **격자 치수·글리프·셀 배치가 컬럼 개수나 항목명에 연동된 곳은
없다.** `FIELD_COLS`/`FIELD_ROWS`는 파일 상수(18/10)이고, 열 인덱스는 오디오 FFT 버킷
(`FFT/FIELD_COLS`)에만 쓰인다. **제품 코드의 시트 스키마 의존 0건.**

---

# ✅ A1·A2 — **민구 선택: 재2배(25×14) + 하단 4행 예약** (08-05)

민구가 세 조합을 목업으로 보고 **「한 번 더 2배 + 하단 4행 예약」** 을 골랐다.
🔑 **네가 발견한 연결이 그대로 채택된 것이다** — *"Q1 안 B를 고르면 Q2 안 B의 대가가 사라진다"*.

| 축 | 확정 |
|---|---|
| 격자 | **18×10(180셀) → 25×14(350셀)** — 각 축 ×√2, 13×7→18×10과 같은 방식 |
| 하단 **4행**(행10~13) | **필 자리로 예약** — 글리프·웨이브가 쓰지 않는다 |
| 실사용 | **10행** — 지금과 같은 세로 해상도. 파형 진폭 상한도 현행 계약 유지 |
| 도트 지름 | 12px → **8.7px** (민구가 대가를 알고 선택했다) |
| 겹침 | **0** |

## 🔴 이 선택이 요구하는 것 (네 조사에 이미 있다)

1. **글리프 4종 재작도**(mic·pause·alert·check) — 25×14 격자로.
   🔴 **켜진 셀 비율을 지금과 비슷하게 유지해라**(발광 면적 중립, 브리핑 §3 G-2 조건).
   before/after 비율을 **세어서** 산출물에 적어라.
2. **`v0440-c5-dots.spec.ts`의 180 리터럴 3곳**(`:5` 주석 · `:32-34` `toBe(180)`) → **350**.
   🔴 **정당 파손이다.** 주석에 *"v0.44.0의 180은 §C5, v0.46.0의 350은 WP-G(민구 확정 08-05)"* 로
   **두 이력을 함께** 남겨라 — 다음 사람이 "왜 또 바뀌었나"를 여기서 읽는다.
3. **하단 4행 예약을 재는 오라클** — `litFromAmps`·글리프 어느 경로로도 행10~13이 켜지지 않는지.
   🔴 **네 상태별 실측표(nav idle/nav wave/paused/anomaly/endReached) 5종을 그대로 케이스로** 써라.
   그게 이 처방의 반증 그물이다.

## ⚠️ 판독성은 실기기 판정 대상으로 남는다

**도트 8.7px가 2~3m 밖에서 읽히는지는 데스크톱에서 판정할 수 없다**(네가 정확히 지적했다).
👉 산출물 「미확인 목록」에 **명시적으로** 올려라: *"도트 지름 8.7px의 야외 판독성 — 실기기 미검증.
민구가 대가를 알고 선택했으나 실사용에서 되돌릴 수 있다(격자 상수 한 곳)."*
**되돌리는 방법을 한 줄로 적어두면** 실기기에서 안 보일 때 민구가 즉시 되돌릴 수 있다.

## 🔑 네 조사에 대한 판정

- **Q1** — 「이미 되어 있다」를 잡아낸 것이 결정적이었다. 브리핑이 **v0.44.0 이력을 모른 채**
  같은 작업을 새 과제로 냈다. 네가 `StateDots.tsx:36-40` 주석과 커밋 `0545fa3`을 짚지 않았으면
  **180셀을 180셀로 다시 만들 뻔했다.**
- **Q2** — *"겹치는 건 글리프가 아니라 웨이브"* 라는 상태별 실측이 처방 자체를 뒤집었다.
  브리핑 §2가 *"원인은 이미 규명됐다, 다시 조사하지 마라"* 라고 못박았는데도 **처방의 대상이
  실측과 다르다는 것을 갈라냈다** — 원인 규명과 처방 검증을 분리한 판단이 옳다.
  🔑 `gates/50` 하단 규칙 그대로다: **진단이 실측이어도 처방은 추론이다.**

---

# 🔴 Q3 (추가, 08-06) — capture 화소 대조가 도트 밀도 증가로 임계를 넘는다

> G-1·G-2는 **완료**다(커밋 `86e5a78` · `0bc093e` · `613a70c`). 이건 **후속 처방 질문**이라
> 작업을 멈추지는 않았다. 다만 처방이 **남의 스펙의 판정력을 낮추는 것**이라 혼자 못 정한다.

## 무엇이 모호한가

`capture-current-states.spec.ts:182`는 프리뷰 카드와 라이브 스크린샷의 **화소 차이 < 1%**를
요구한다. 격자가 180 → 350셀이 되면서 **애니메이션 위상차가 만드는 차이 화소가 비례로 늘어**
두 케이스가 임계를 넘는다.

| 케이스 | 도트 상태 | 호흡 주기 | 화소 차이 | 판정 |
|---|---|---|---|---|
| `01-active-listening` | idle 웨이브(위상 흐름) | — | **1.6%** | ❌ |
| `04-anomaly` | alert 글리프 + mono 점멸 | **0.7s(최속)** | **3.84%** | ❌ |
| `02`·`03-active-*level` | 파형 | — | 0% | 🟢 |
| `07-paused` | pause 글리프 | 2.4s(최저속) | 0.2% | 🟢 |
| `06-panel-open` | **도트 없음** | — | 0% | 🟢 |

👉 **도트가 없으면 0%, 호흡이 느리면 0.2%, 가장 빠르면 3.84%.** 원인은 도트 애니메이션이고,
셀 수가 1.94배가 되면서 같은 위상차가 만드는 화소가 그만큼 늘었다. **내 변경의 직접 결과다.**

🟡 참고 — 같은 스펙의 다른 2건은 **내 축이 아니다**: `05-anomaly-corrected`의 drift 4건은 전부
칩존(`column-chip` rect.x Δ53px · 칩존 `scrollLeft` 594→541), `09-chipzone-overflow` 2.29%도
같은 축이다 → **WP-D 소관.**

## 후보 (고르지 않았다)

| | **안 A — 임계를 올린다** | **안 B — 캡처 시 애니메이션을 멈춘다** |
|---|---|---|
| 무엇 | `:182`의 `toBeLessThan(1)` → `5` 정도 | 라이브·프리뷰 양쪽 캡처 직전에 `animation-play-state: paused` 주입 |
| 🟢 | 한 줄. 도트 외 축(레이아웃·색·문구)은 그대로 잡힌다 | **근본 해결** — 위상차가 0이 되어 임계 1%를 지킬 수 있다 |
| 🔴 | **오라클 약화** — 5% 안에 숨을 수 있는 진짜 회귀가 생긴다 | 캡처 인프라(`previewCapture.ts`) 변경 · 정지된 화면이 민구가 볼 프리뷰로 적절한지 별도 판단 |
| 범위 | 남의 스펙 1줄 | 남의 픽스처 |

## 왜 혼자 못 정하나

- 두 안 다 **`capture-current-states`의 판정력을 바꾼다.** 이 스펙의 존재 이유가
  *"어긋난 카드를 통과시키면 민구가 잘못된 화면을 보고 결정하게 되고 그 결정이 통째로 무효가
  된다"*(스펙 상단 원문)라서, 임계를 무르는 건 그 계약을 깎는 결정이다.
- **프리뷰는 민구가 눈으로 보는 산출물**이다. 애니메이션을 멈춰 캡처하는 게 맞는지는
  화면을 보는 사람이 정할 축이다.

## 막힌 범위

**없다 — G-1·G-2는 완료했고 커밋했다.** 이 두 케이스는 `capture-current-states`의 화소 대조
단언만 red이고, **도트 마스크·overflow 진단은 전부 통과했다**(`dots` 합 350 · `overflow=0/0/0/0` ·
`04-anomaly`의 `glyph/50/0/300`으로 alert 재작도까지 확인됨).

## 🔴 Q3 정정 (08-06, 실측 후) — **귀책이 나에게 있지 않다. 임계를 올리면 안 된다.**

> 위 Q3는 *"내 변경의 직접 결과다"* 라고 썼다. **그건 추론이었고 측정하지 않았다.**
> 지적을 받고 판별 실험을 했더니 **틀렸다.** 아래가 실측이다.

### 판별 실험 — 격자만 v0.45.0으로 되돌려 같은 케이스를 잰다

`StateDots.tsx`의 `FIELD_COLS`/`FIELD_ROWS`만 `18`/`10`으로 되돌리고(내 주석이 문서화한 바로
그 롤백 지점) `capture --grep "01-active-listening"` 실행:

```
[capture] 01-active-listening: drift=0 unstable=0 px=1.34% nodes=13
          dots=idle/92/0/88 overflow=0/0/0/0 hits=input-control-toggle:true
```
`dots` 합 = **180** → 롤백이 실제로 걸렸다. 그런데 **화소는 1.34%로 여전히 임계 1%를 넘는다.**

| | v0.45.0 격자(18×10) | v0.46.0 격자(25×14) | 내 기여 |
|---|---|---|---|
| `01-active-listening` 화소 | **1.34%** | 1.6% | **+0.26%p** |

👉 **내 변경 없이도 이미 red다.** 격자 밀도의 기여는 0.26%p로 임계 초과분(0.34%p)보다 작다.

### 처음 추론이 왜 틀렸나 (기록으로 남긴다)

- **반증이 표 안에 이미 있었다** — `02`·`03`(wave)이 **0%**다. wave 셀도 `dot-breathe`를 달고
  가장 동적인 모드인데, *"위상차가 셀 수에 비례한다"* 가 기전이라면 **wave가 최악이어야 한다.**
  내 표는 밀도가 전부를 설명하는 것처럼 읽히게 배열돼 있었다.
- **`01`·`04`에는 칩존이 들어 있다.** 그리고 `05`에서 칩존 drift가 이미 확인됐다
  (`column-chip` rect.x **Δ53px** · `scrollLeft` 594→541). 칩 화소가 `01`의 1.6% 중 대부분일 수 있다.

### 🔴 그래서 처방이 뒤집힌다

**안 A(임계를 1→5%로 올린다)를 고르면 남의 회귀를 가린다.** 위 실측이 나오기 전이라
후보로 올렸는데, **지금은 반대한다.** `capture-current-states`의 존재 이유가 *"어긋난 카드를
통과시키면 민구가 잘못된 화면을 보고 결정하게 되고 그 결정이 통째로 무효가 된다"* 인데,
지금 red는 **그 오라클이 제 일을 하고 있다는 신호**로 읽어야 한다.

👉 **WP-G는 이 건에 처방을 내지 않는다.** 칩존 축을 보는 레인(WP-D)이나 오케스트레이터가
`01`·`04`·`05`·`09`를 한 묶음으로 봐야 한다. 내가 낼 수 있는 건 **판별 실험 결과**까지다.

### 함께 남기는 실측 — `v035-hero-confirm` 2건도 내 축이 아니다

`:175`·`:252`는 **격리 재실행에서도 재현**된다(flaky 아님, 2 failed / 3 passed). 다만 이 스펙의
`state-dots` 참조는 `toBeVisible()` 3곳뿐이라 **격자 크기와 무관**하고, 깨지는 단언은
`toHaveText("30.7")`·`review 입력값 "4.2"`·`column-chip data-active` = **값·칩 축**이다.
