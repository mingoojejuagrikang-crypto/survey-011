# _ASK — 레인 R1 (v0.46.0 패치 리뷰, 콜드 컨텍스트)

산출물: `~/workspace_teamops/deliverables/2026-08-07-survey-011-v0460-patch-review.md`
상태: **판정 완료.** 아래는 **차단 질문이 아니다** — 진행을 멈추지 않았고, 전부 산출물에 반영돼 있다.

---

## 1. 🔴 브리핑 전제 반증 — 인용 실측의 출처를 찾지 못했다

브리핑 §「이 패치가 닫으려는 것」 L3-2 항목이 이렇게 적었다:

> `reachable()`가 조상 히트를 통과시켜 클리핑 실명 … **667×375 실측: strict=false인데 loose=true —
> `elementFromPoint`가 `input-control-panel`을 돌려주는데 HIT로 읽혔다**

**확인 결과:**
- 콜드 리뷰 산출물 `2026-08-06-…-cold-review.md` §8의 L3 항목(114행)은 **코드 판정식만** 근거로 든다.
  `667×375`·`input-control-panel`·`strict/loose` 문자열이 그 문서에 **없다.**
- `v0460-fb-b-drawer-reachable.spec.ts`의 뷰포트는 **402×812 · 402×874 · 375×667** 셋뿐이다.
  **667×375(가로)는 이 스펙에 없다.**
- 오라클을 조인 뒤 **3뷰포트 전부 `행히트=true` · `+버튼히트=true`** (4 passed / ✘0).

👉 **그 실측이 어느 스펙·어느 실행에서 나온 것인지 알려주면** 「조인 오라클이 제품을 통과한다」는
내 판정을 그 조건에서 한 번 더 재보겠다. 지금은 산출물에 **🔵 출처 미확인**으로 적어뒀다.

⚠️ 이것이 판정을 바꾸지는 않는다 — L3-2 처방(`top.contains(el)` 제거) 자체는 옳고 제품도 통과한다.

---

## 2. 🟡 브리핑 전제와 실측이 갈린 곳 하나 더 — §9 「정당 파손 예상표」의 E 항목

예상표 E: *"총 행 수를 단정하는 스펙 **전부**(50→10)"*
**실제 파손 0건.** 전수 확인 결과 기존 스펙의 `input:'touch'` 컬럼은 **모두 `auto:{kind:'fixed'}`**라
이미 `isCycling === false`였다. **순환 대상 수동 컬럼을 seed하는 스펙이 하나도 없었다.**

👉 이건 처방이 안전했다는 뜻이기도 하지만, 동시에 **E의 술어 변경을 재는 기존 오라클이 사실상
없었다**는 뜻이다. 신규 `v0460-cr-data-contract.spec.ts`가 없었다면 이 변경은 **어떤 게이트에도
걸리지 않고** 지나갔다.

---

## 3. 🔵 못 연 문서 — 「미검증」으로 적었다

- `sheets.ts`의 실측표(조사과실 5 / 농가 21 / 메모 2902 · 2,902행)가 인용하는
  **`08-05 WP-A 진단 §시트 실측`** 원문 — 볼트 경로에 접근하지 않았다.
  **산술은 정합**(2902/5=580.4 · 2902/21=138.2)하지만 **원출처는 확인 못 했다.**
- `VoiceHero.tsx:349`의 A/B 종전값 **76.38px · 카드 256px** — 재현 불가.
  회차 SSOT §12 표는 패치 후 **141.8px**을 *"L3이 잰 정상값 그대로"*라고 적는다.
  **두 문서가 서로 다른 값을 「정상」이라 부른다** — L3 원산출물을 열면 갈린다.

---

## 4. 🔴 운영 알림 — **이 워크트리에 다른 실행 주체가 붙어 있다**

리뷰 중(06:41~07:19) `~/projects/survey-011`에 **내가 만들지 않은 변경 3건**이 생겼다:

```
 M Deliverables/assets/2026-08-02-ui-e4/alarm-402x874.png        06:56:22
 M Deliverables/assets/2026-08-02-ui-e4/exit-inline-375x650.png  07:13:10
 M Deliverables/assets/2026-08-02-ui-e4/exit-inline-402x874.png  07:13:10
```

이 경로에 쓰는 스펙은 **`trend-alert.spec.ts:434`** 와 **`v043-exit-inline.spec.ts:49,52`** 뿐이고
**나는 둘 다 실행하지 않았다.** 세션 시작 시 `git status`에도 없었다.
오늘 `worklogs/survey-011/`에 `[REVIEW-R1]` 외의 레인 기록은 **0건**이다.

👉 **누가 같은 워크트리에서 돌리고 있는지 확인이 필요하다.**
- 🟢 포트는 안 겹쳤다(9회 전부 `프리플라이트 OK — :5186` 통과).
- 🟡 **CPU는 공유됐다** — 내 판정은 `--workers=1`이었으나 「단독 실행」은 아니었다.
  크기·값 측정 판정(`alarm-card-floor` · `drawer-reachable` · 순수 유닛)은 부하 무관이지만,
  **타이밍 임계 판정 1건**(`blackout-escape`의 270ms 단타)은 부하로 rAF가 느려지면 더 쉽게 통과한다.
- 🟢 **내가 해소했다** — 다른 세션의 마지막 기록(07:13) 이후 조용한 환경에서 재실행해
  **4 passed · ✘0**. 오케스트레이터가 따로 확인할 필요 없다.
- ⚠️ **더티 워크트리 보존 계약대로 건드리지 않았다** — 검사만 했다.

---

## 5. ⏭ 판정에 필요하나 내 권한 밖인 것 (읽기 전용 계약 준수)

- **R1-3**: `predeploy:preview`에 `test:e2e`를 넣을지 말지는 **속도 대 안전의 사양 결정**이다.
  현행은 정적 검사만 붙었고 회귀 게이트는 여전히 우회된다. 판단이 필요하다.
- **R1-2 처방 후보 4번**: 「인식값이 뜨면 카드가 절반」을 **의도된 배분으로 확정**할 수도 있다.
  그 경우 R1-2는 종결되고 `CARD_MIN_H`의 근거만 문서화하면 된다. **실기기에서 민구가 보고 판단할 축.**
- **R1-1**: 별건 critical 등재 대상. 처방 후보 4개를 산출물에 적어뒀고 **고르지 않았다.**
