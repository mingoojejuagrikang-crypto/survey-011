# _ASK-wp-a — WP-A 레인 질문

> 등급: 🟡 **NON-BLOCKING** — 코드 판정은 완료했고 산출물도 제출했다.
> 아래는 **원인 갈래(4a/4b)를 확정**하고 확신도를 "코드상 도달 가능" → "실제 발동 확인"으로 올리는 데
> 필요한 관측이다. 답이 없어도 산출물은 유효하며, 답이 오면 §2 경로4의 "안 잰 것"이 닫힌다.
>
> 산출물: `~/workspace_teamops/deliverables/2026-08-05-survey-011-v0460-WP-A-diagnosis.md`

---

## Q1. 오알람 원인이 4a인가 4b인가 — 시트 한 번으로 갈린다

### 무엇이 모호한가

F3의 원인을 **경로 4(유효 키 컬럼 집합이 실제 샘플 구분자보다 좁아짐)**로 확정했다.
그런데 경로 4에는 **관측상 구별되지 않는 두 갈래**가 있고, **처방이 서로 다른 층을 건드린다.**
둘 다 코드에 실재하므로 코드만 더 읽어서는 갈리지 않는다 — 민구 환경의 실값이 있어야 갈린다.

### 후보 (두 갈래를 나란히 — 고르지 않았다)

| | **4a — 숫자 키 컬럼이 키에서 탈락** | **4b — options 키 컬럼이 첫 값에 못박힘** |
|---|---|---|
| 코드 지점 | `sheets.ts:220-236` (int/float 유추) | `sheets.ts:245-247, 251-252` (text→options 유추) |
| 발동 조건 | 조사나무·조사과실의 시트 고유값이 **연속수열이 아님**(예: 3·7·12·15) → `input='voice'` → `inferSampleKey` false → 키에서 빠짐 | 라벨·처리가 `options`로 유추되며 `selected = available.slice(0,1)` = **첫 값 1개만** 선택된 채로 남음 |
| 오알람이 생기는 방식 | 키가 굵어져 **다른 나무/과실**이 한 키를 공유 → `pastValues.ts:162` 마지막 행 승리 값과 비교 | 키 길이는 정상인데 **라벨 조각이 틀린 값** → 지난 회차의 **다른 구역** 행과 매칭 |
| 시트에 남는 흔적 | 라벨은 맞게 올라감 | 🔴 **틀린 라벨이 그대로 업로드됨**(= 데이터 오염) |
| 사용자가 알아챌 표면 | 없음 | 없음 — `ttsAnnounce=false`(`sheets.ts:252`)라 음성 안내도 없음 |
| 처방이 건드리는 층 | 타입/입력 유추 규칙 (블래스트 반경 **큼** — 측정 컬럼까지 통과하는 분기) | options 기본 선택값 + 안내 (블래스트 반경 **중간**) |

### 왜 혼자 못 정하나

민구 기기의 **persist된 컬럼 설정**(`input`·`sampleKey`·`auto.selected`)은 폰 로컬 저장소에 있고,
**시트 실데이터**도 이 레인에서 접근 경로가 없다. 코드로는 "두 경로가 모두 도달 가능하다"까지가 한계다.
임의로 한쪽을 골라 처방을 세우면, 틀렸을 때 **블래스트 반경이 큰 쪽(4a 처방)이 측정 컬럼의 음성 입력을
망가뜨릴 수 있다** — 되돌리기 비싼 방향이라 추측으로 진행하지 않았다.

### 필요한 관측 3건 (하나만 와도 진전된다)

1. **오알람이 났던 행에 시트로 업로드된 「라벨」 값** — 실제 조사 구역과 **다르면 4b, 같으면 4a**.
   👉 이 한 건이 가장 값지다.
2. **시트 「조사나무」·「조사과실」 열의 고유값 목록** — 연속(1,2,3…)인지 띄엄띄엄인지.
   비연속이면 4a 발동 조건이 충족된 것이다.
3. **설정탭 컬럼 목록 스크린샷** — 각 컬럼의 입력방식(자동/음성/터치)과 샘플키 토글 상태.
   조사나무·조사과실이 「음성」으로 되어 있으면 4a 확정.

### 막힌 범위

없다. 산출물은 제출 완료. 이 답이 오면 산출물 §2 경로4의 "안 잰 것 ①②③"과 §5 U1·U2·U3가 닫히고,
처방 후보 중 하나를 근거를 갖고 고를 수 있다.

---

## Q2. 계측 로그에 샘플키 **실값**을 남겨도 되는가 (개인정보 판단)

### 무엇이 모호한가

산출물 §4의 계측 설계에서 `sample_key`(조회에 쓴 키 문자열)를 남기자고 제안했다.
이 값은 **농가명·라벨 실값**이다. 로그가 어디까지 나가는지(기기 내부 only / 외부 수집)에 따라
남길 수 있는 범위가 달라진다.

### 후보 (두 안 나란히 — 고르지 않았다)

- **안 A — 실값 그대로.** 사후 판정력이 가장 높다(경로 2의 공백 충돌까지 눈으로 확인 가능).
  공백을 `·`로 치환해 경계가 보이게 남긴다.
- **안 B — 값은 해시/길이만, 컬럼 이름 목록은 실명.** 4a 판정(키 목록이 짧아졌는지)은 **그대로 가능**하다.
  잃는 것은 경로 2(공백 충돌) 사후 판정력뿐이다.

### 왜 혼자 못 정하나

로그 수집 범위와 개인정보 판단은 이 레인의 권한 밖이다. 다만 **어느 안이든 4a 판정에는 지장이 없다** —
결정적 필드는 키 문자열이 아니라 **키 컬럼 이름 목록 + 각 `effectiveSampleKey`**이기 때문이다.

### 막힌 범위

없다. 계측은 이번 레인에서 **설계만** 하고 구현하지 않으므로(브리핑 §4-4 계약), 이 답은 구현 착수 시점에
필요하다. 지금 진행을 막지 않는다.

---

## 답변란 (Larry / 민구가 여기에 적어주세요)

- **A1:** 🔴 **민구에게 물었다 — 답 대기 중.** (관측 3건 중 ①「오알람 난 행의 시트 라벨 값」이
  가장 값지다는 네 판단에 동의한다. 그 한 건으로 4a/4b가 갈린다.)
  ⚠️ **답이 오기 전에 처방을 구현하지 마라.** 네 판단이 옳다 — 4a 처방은 타입/입력 유추 규칙을
  건드려 **측정 컬럼의 음성 입력까지 통과하는 분기**를 바꾼다. 블래스트 반경이 큰 쪽을
  추측으로 고르지 않은 것이 정확한 행동이었다.

- **A2:** 🟢 **안 A(실값 그대로, 공백은 `·`로 치환) 채택.** 근거 3가지 — **다만 실측으로 반증되면
  그쪽이 이긴다:**
  1. **로그는 민구 본인 소유 데이터다.** IDB에 쌓이고 zip으로 **민구 자신의 Drive**에 올라간다
     (`logger.ts:3` *"auto-uploaded ZIP"*). 제3자 수집 경로가 없다.
  2. **같은 민감도의 값이 이미 로그에 있다** — `type:'value'` 이벤트가 `text`(발화 원문)와
     `parsed`를 남긴다(`useVoiceSession.ts:1034·2274·2370`). 샘플키 실값이 새로 여는 노출 축이 아니다.
  3. **농가명은 민구의 업무 데이터**이지 제3자 개인정보가 아니다.
  🔑 **`·` 치환은 반드시 해라** — 경로 2(구분자 공백 충돌)의 사후 판정이 그것 하나에 달려 있다.
  `신례리 1365-1`이 `신례리·1365-1`로 보이면 한 값이고, `신례리·1365-1`이 두 칸으로 보이면 충돌이다.
  ⚠️ 계측 **구현은 이번 레인 밖이다**(브리핑 §4-4 계약대로 설계만). 이 답은 구현 회차가 쓴다.
