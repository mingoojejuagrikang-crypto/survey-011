# 🔴🔴 STOP — WP-A2 사양 철회 (Larry, 민구 정정 08-05)

> **지금 하던 구현을 중단해라.** 아직 코드를 안 만졌으면 **시작하지 마라.**
> 만졌으면 **커밋하지 말고 그 상태로 두고** 아래를 읽어라. (🔴 `git checkout`/`reset`으로
> 되돌리지 마라 — 다른 레인의 변경이 같은 트리에 있다.)

## 무엇이 철회됐나

**브리핑 §2의 확정 사양 전체** — 「직전 조사일에 없던 샘플은 무알람」(A안)과
그 구현안(형제 그룹의 직전 조사일 · `groupRounds` 맵)을 **철회한다.**

## 민구 정정 원문 (08-05)

> *"논리로는 각 과실 4/5가 직전에 없더라도 **마지막 값이 작년에 있다면 작년 것 값이 직전 값이
> 되는 게 논리가 맞아.** 그리고 해당 논리로 정정해줘. 직전 작기와 헷갈리는 부분은 **내가 시트에
> 재배 작기를 추가해서 작년 자료와 헷갈리지 않게 했어야 했어.**"*

## 🔑 이것이 뜻하는 것

| | 철회 전 (틀림) | 정정 후 (맞음) |
|---|---|---|
| `previousRound`의 동작 | 고쳐야 할 것 | 🟢 **논리적으로 옳다. 그대로 둔다** |
| 과실 4·5가 23일 전 값과 비교된 것 | 버그 | 🟢 **정상 동작** — 그게 그 샘플의 진짜 직전 값이다 |
| 진짜 문제 | 앱 로직 | 🔑 **시트에 「재배 작기」 구분 컬럼이 없다** — 작년 작기와 올해 작기가 한 시트에 섞여 있다 |
| 해결 주체 | 코드 | **민구의 데이터 설계**(작기 컬럼 추가) |

🔴 **즉 F3는 앱 버그가 아니었다.** 진단(§6-1~§6-3의 실측)은 유효하고, **처방(§6-4·§6-5)만 무효다.**

## 지금 할 일

1. **코드 수정을 중단**하고 `wlog`로 현재 상태를 보고해라
   (`wlog "[WP-A2] 사양 철회 수신 — 중단. 현재 <무수정 / N파일 수정, 미커밋>"`).
2. 이미 만든 것이 있으면 **무엇을 만들었는지만** 산출물 초안에 적어라(버리지 말고 기록).
   `groupRounds` 설계는 나중에 「작기 구분」 기능이 생기면 참고가 될 수 있다.
3. **그 다음 지시를 기다려라.** Larry가 민구에게 새 범위를 확인하는 중이다.
   기다리는 동안 아래 조사는 해도 된다(**코드 수정 없음**):
   - `previousRound`가 **몇 일 전 값과 비교했는지**를 알람 팝업·TTS·로그에서 지금 알 수 있는가?
     (`anomalyAlertContext`가 `prevDate`를 이미 준다 — **화면에 실제로 표시되는지** 확인)
   - 그 표시가 없다면 **어디에 넣을 수 있는지** 후보 지점만 적어라(구현 금지).
   🔑 새 범위가 그쪽일 가능성이 높다 — 사용자가 *"이건 작년 값과 비교한 거구나"* 를 **알아챌 수
   있게** 하는 것.

**A: 🟢 WP-A는 코드 변경 없이 닫힌다. 이 레인은 종료다.**

**확인한 것(Larry 실측):** 알람 팝업이 **직전 회차 날짜를 이미 표시하고 있다** —
`AnomalyAlertPopup.tsx:142` `data-testid="anomaly-prev-label"`, `formatCompareDate()`가
`07-13` 형식(월-일)으로 그린다. 👉 **표시 공백도 아니었다.** 남은 개선 여지(경과일수 「23일 전」
병기)는 알람 카드가 fit 압박 지대라 **WP-B의 몫**이고, 이번 범위가 아니다.

**네가 할 일:** 아무것도 없다. 산출물도 쓰지 마라 — Larry가
`deliverables/2026-08-05-survey-011-v0460-WP-A-diagnosis.md` **§7**에 정정 전체를 기록했다.
`wlog`로 종료만 알리고(`wlog "[WP-A2] 종료 — 코드 변경 없음 확정(민구 정정). 소스 무수정"`) 끝내라.

🔑 **네가 아무것도 안 만든 것이 옳은 결과다.** 사양이 두 번 뒤집히는 동안 트리를 깨끗하게
유지했고, 그래서 되돌릴 것이 없다.
